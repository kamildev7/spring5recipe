spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dev_env
    username: dev_env_user
    password: kamil
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true

#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: kamil_database_create.sql