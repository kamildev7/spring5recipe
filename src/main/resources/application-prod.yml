spring:
  datasource:
    url: jdbc:mysql://localhost:3306/prod_env
    username: prod_env_user
    password: kamil
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false